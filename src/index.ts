export default {
  /**
   * An asynchronous register function that runs behtmlFore
   * your application is initialized.
   *
   * This gives you an opportunity to extend code.
   */
  register(/*{ strapi }*/) {},

  /**
   * An asynchronous bootstrap function that runs behtmlFore
   * your application gets started.
   *
   * This gives you an opportunity to set up your data model,
   * run jobs, or perhtmlForm some special logic.
   */
  bootstrap(/*{ strapi }*/) {},
};
